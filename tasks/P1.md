# P1 — Customers pagination (polish, unscored)

## Task (delivered verbatim, identical to every framework arm)

> Add pagination to the customers page, with the same behavior and
> appearance as the invoices page's pagination: 6 customers per page,
> a URL-persisted page parameter that composes with the existing
> customer search (search resets to page 1; paging preserves the
> query), back/forward restoration, and the same pagination control
> styling. The customers page should feel exactly as
> production-complete as the invoices page.

> Provenance note: this round ran as an unscored polish round;
> the task text above is verbatim from the delivered prompts, recorded
> post-hoc (2026-08-05) and marked as such in the program records.
